import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.waaps.deliveryist',
  appName: 'deliveryist',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
